import React from 'react';

const FaviconGenerator = () => {
  // Colors from your variables.css
  const primaryColor = '#BF9D5E'; // Gold
  const secondaryColor = '#0E5936'; // Dark green

  return (
    <div className="flex flex-col items-center p-8 bg-gray-100 rounded-lg">
      <h2 className="text-2xl font-bold mb-6">Accenty & Co Favicon</h2>
      
      <div className="mb-6">
        <svg width="64" height="64" viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg">
          {/* Background circle */}
          <circle cx="32" cy="32" r="32" fill={secondaryColor} />
          
          {/* "A" letter */}
          <text 
            x="32" 
            y="42" 
            fontFamily="Arial, sans-serif" 
            fontSize="40" 
            fontWeight="bold" 
            textAnchor="middle" 
            fill={primaryColor}
          >
            A
          </text>
          
          {/* "&" symbol */}
          <text 
            x="50" 
            y="26" 
            fontFamily="Arial, sans-serif" 
            fontSize="16" 
            fontWeight="bold" 
            textAnchor="middle" 
            fill={primaryColor}
          >
            &
          </text>
        </svg>
      </div>
      
      <div className="bg-white p-4 rounded-lg shadow-md">
        <h3 className="text-lg font-semibold mb-4">Instructions:</h3>
        <ol className="list-decimal pl-5 space-y-2">
          <li>Save this SVG by right-clicking on it and selecting "Save Image As..."</li>
          <li>Use an online converter like <a href="https://convertio.co/svg-ico/" className="text-blue-600 hover:underline" target="_blank" rel="noopener noreferrer">convertio.co</a> to convert the SVG to ICO format</li>
          <li>Name the file <code className="bg-gray-200 px-2 py-1 rounded">favicon.ico</code></li>
          <li>Place the file in your project's <code className="bg-gray-200 px-2 py-1 rounded">public</code> directory</li>
        </ol>
      </div>
    </div>
  );
};

export default FaviconGenerator;